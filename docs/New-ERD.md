التصميم النهائي والمُعتمد لجداول قاعدة البيانات (ERD) ، مجهز للعمل مباشرة على **PostgreSQL**.

---

### 1️⃣ الجدول الأول: `users` (المديرين/الكُتّاب)
هذا الجدول مسؤول عن تخزين بيانات من لهم صلاحية الدخول للنظام.

| اسم العمود | نوع البيانات (PostgreSQL) | الوصف والقيود (Constraints) |
| :--- | :--- | :--- |
| **`id`** | `SERIAL` | **مفتاح أساسي (PK)**. (رقم يتزايد تلقائياً 1، 2، 3...) |
| **`username`** | `VARCHAR(100)` | اسم المستخدم (للعرض). |
| **`email`** | `VARCHAR(255)` | البريد الإلكتروني. **(يجب أن يكون UNIQUE - غير مكرر)**. |
| **`password_hash`** | `VARCHAR(255)` | كلمة السر المشفرة (Hashed) وليست الأصلية. |
| **`created_at`** | `TIMESTAMP` | تاريخ إنشاء الحساب (يُملأ تلقائياً). |

---

### 2️⃣ الجدول الثاني: `claims` (الإدعاءات/المقالات)
هذا الجدول هو مخزن المحتوى الرئيسي.

| اسم العمود | نوع البيانات (PostgreSQL) | الوصف والقيود (Constraints) |
| :--- | :--- | :--- |
| **`id`** | `SERIAL` | **مفتاح أساسي (PK)**. |
| **`title`** | `VARCHAR(255)` | عنوان الإدعاء الرئيسي. |
| **`summary`** | `TEXT` | ملخص قصير يظهر في القائمة الخارجية. |
| **`image_url`** | `VARCHAR(500)` | رابط الصورة (التي تم رفعها على الـ CDN). |
| **`content`** | **`JSONB`** | **(السر هنا)**: يخزن قائمة الفقرات والعناوين كبنية بيانات كاملة. |
| **`author_id`** | `INTEGER` | **مفتاح أجنبي (FK)**. يربط هذا الإدعاء بصاحبه في جدول `users`. |
| **`created_at`** | `TIMESTAMP` | تاريخ ووقت نشر الإدعاء (يُملأ تلقائياً). |

---

### 🔗 شكل العلاقة (The Relationship)

العلاقة هنا هي **One-to-Many** (واحد لمتعدد):

> **User (1)** -------- **< يكتب >** -------- **(N) Claims**

*   **User:** يمكنه كتابة مقالات كثيرة.
*   **Claim:** يملكه كاتب واحد فقط (المحدد في `author_id`).
